services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    container_name: vecired-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
